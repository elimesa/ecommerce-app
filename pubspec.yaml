name: ecommerce_app
description: a new Flutter app using the BLoC pattern and firebase as database

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc: ^7.3.0
  cloud_firestore: ^2.5.3
  firebase_storage: ^10.0.3
  firebase_core: ^1.7.0
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
